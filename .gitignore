# Compiler files
cache/
out/

# Broadcast logs: excluded for the clone experiment (real-run tx records are cited in
# script/FINDINGS.md; keeping the raw run-*.json out of the repo avoids env-specific clutter).
/broadcast/

# Per-run clone deployment addresses — generated by `make clean-state` + deploy; not committed
# (a colleague generates their own on first deploy).
/script/clone.eth.json
/script/clone.avax.json

# Docs
docs/

# Dotenv file
.env

# Local-only scratch / shelved files
tmp/
